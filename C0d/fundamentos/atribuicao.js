const a = 7  // = é atribuição não comparação
let b = 3

b += a //atribuição aditiva, acrecentando o valor de 'a' variavel 'b'  b = b + a
console.log(b)

b -= 4 // b = b - 4
console.log(b)

b *= 2 // b = b * 2
console.log(b)

b /= 2 // b = b / 2
console.log(b)

b %= 2 // b = b % 2 (resto da divisão) se for par = 0 impar é diferente
console.log(b)
